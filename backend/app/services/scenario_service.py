"""Phase 5 TODO: what-if simulator reusing the driver model (roadmap section 24)."""

from app.schemas.common import NotImplementedResponse


def run_scenario(payload: dict) -> NotImplementedResponse:
    return NotImplementedResponse(
        feature="scenario",
        phase="Phase 5 — Scenario engine",
        reason="Scenario simulation reuses the driver model, which is not implemented yet.",
    )
