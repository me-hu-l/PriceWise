# Phase 2 TODO: define ForecastRead, ForecastContributionRead schemas here
# (point_forecast, lower_bound, upper_bound, direction, confidence_score, driver contributions)
# once app/ml/* produces real forecasts. Phase 1 exposes only NotImplementedResponse
# (see app/schemas/common.py) for forecast-related endpoints.
